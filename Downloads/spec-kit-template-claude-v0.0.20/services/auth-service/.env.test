# Test Environment Configuration
NODE_ENV=test
PORT=3003

# Database (use separate test database)
DB_HOST=localhost
DB_PORT=5432
DB_USER=research_admin
DB_PASSWORD=research_secure_pwd_2025
DB_NAME=research_study_test_db
DATABASE_URL=postgresql://research_admin:research_secure_pwd_2025@localhost:5432/research_study_test_db

# Redis (use separate test database)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=1
REDIS_URL=redis://localhost:6379/1

# Authentication (use test secrets)
JWT_SECRET=test_jwt_secret_for_testing_only
JWT_EXPIRATION=1h
JWT_REFRESH_SECRET=test_refresh_secret_for_testing_only
JWT_REFRESH_EXPIRATION=24h
BCRYPT_ROUNDS=4

# Logging
LOG_LEVEL=error

# Rate Limiting (more permissive for tests)
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=1000

# Disable certain features in tests
AUDIT_LOG_ENABLED=false
PHI_ENCRYPTION_ENABLED=false